version: '3'
services:
  database:
    image: mysql
    container_name: database
    environment:
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: achatdb
    ports:
      - 3306:3306
    volumes:
      - data:/var/lib/mysql
    networks:
      - mynet

  spring-boot:
    image: dqntero/spring
    container_name: back
    ports:
      - 8089:8089
    depends_on:
      - database
    restart: on-failure
    networks:
      - mynet
volumes:
  data:
networks:
  mynet:
    driver: bridge
